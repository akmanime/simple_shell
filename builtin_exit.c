#include "shell.h"
/**
 * shell_exit - exits the shell
 * @argu: Structure containing potential arguments. Used to maintain
 * constant function prototype.
 * Return: Always returns -1 to indicate that the shell should exit.
 */
int shell_exit(argu_t *argu)
{
	(void)argu;

	return (-1);
}
